# Requirements pour les tests CI
# Compatible Python 3.12
# Optimisé pour réduire l'espace disque sur GitHub Actions runners

# API Framework
fastapi>=0.118.0
uvicorn>=0.23.0

# Machine Learning Core (Python 3.12 compatible)
numpy>=1.26.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Deep Learning - Version CPU uniquement (sans CUDA)
tensorflow-cpu==2.20.0
tensorflow-hub>=0.16.1

# Text Processing & NLP (Python 3.12 compatible)
nltk>=3.9.0
gensim>=4.3.2

# Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0

# MLOps & Tracking (Python 3.12 compatible)
mlflow>=2.16.0

# Testing & Quality
pytest>=8.0.0
pytest-cov>=4.0.0
black>=24.0.0
flake8>=7.0.0
mypy>=1.8.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0

# Note: PyTorch et transformers ne sont pas inclus pour économiser l'espace disque
# Les tests BERT ne sont pas exécutés dans le pipeline CI